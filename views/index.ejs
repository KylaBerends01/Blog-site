<div class="row flex-lg-row-reverse align-items-center g-5 py-4 mb-4">
    <div class="col-12 col-lg-6">
        <img src="https://i.ibb.co/4YNnvcd/hero-imag.png" class="d-block mx-lg-auto img-fluid" width="607" loading="lazy" alt="hero-imag">
    </div>
    <div class="col-12 col-lg-6"></divclass>
        <h1 class="display-5 fw-bold mb-3">Information Library for Videography Lovers</h1>
        <p class="lead">
            Welcome to our blog page for videographers! Our team of experts share valuable insights, tips, and resources to help you elevate your videography skills. 
        </p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <a href="/explore-latest" class="btn btn-primary btn-dark btn-lg px-4 me-md-2">Explore Latest</a>
            <a href="/explore-random" class="btn btn-secondary btn-lg px-4 me-md-2">Show Random</a>
        </div>
    </div>
</div>


<!-- categories start -->
<div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3 py-4">

    <% if(categories != '') { %>
        <% categories.forEach(function(category,index) { %>
            <a href="/categories/<%- category.name %>" class="col text-center category__link">
                <div class="category__img shadow">
                    <img src="<%- category.image %>" alt="<%- category.name %>" loading="lazy">
                </div>
                <div class="pt-1"><%- category.name %></div>
            </a>
        <% }) %>
    <% } %>

    <a href="/categories" class="col text-center category__link">
        <div class="category__img shadow">
            <img src="https://i.ibb.co/rffGNFg/view-all.png" alt="View all categories" loading="lazy">
        </div>
        <div class="pt-1">View all</div>
    </a>
</div>
<!-- categories end -->





<!-- latest start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Latest Blogs</h2>
        <a href="/explore-latest" class="ms-auto">View more</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(content != '') { %>
            <% content.latest.forEach(function(blog, index) { %>
                <a href="/blog/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found</p>
        <% } %>
    </div>
</section>
<!-- latest end -->


<!-- drone start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Drone Blogs</h2>
        <a href="/categories/Drone" class="ms-auto">View more</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(content.drone != '') { %>
            <% content.drone.forEach(function(blog, index) { %>
                <a href="/blog/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found</p>
        <% } %>
    </div>
</section>
<!-- drone end -->

<!-- Photos start -->
<section class="pb-4 pt-4">
    <div class="d-flex mb-2 align-items-center">
        <h2>Photo Blogs</h2>
        <a href="/categories/Photos" class="ms-auto">View more</a>
    </div>

    <div class="row row-cols-2 row-cols-lg-5 g-2 g-lg-3">
        <% if(content.photos != '') { %>
            <% content.photos.forEach(function(blog, index) { %>
                <a href="/blog/<%= blog._id %>" class="col text-center category__link">
                    <div class="category__img category__img--large shadow">
                        <img src="<%= blog.image %>" alt="<%= blog.name %>" loading="lazy">
                    </div>
                    <div class="pt-1"><%= blog.name %></div>
                </a>
            <% }) %>
        <% } else { %>
            <p>No items found</p>
        <% } %>
    </div>
</section>
<!-- Photos end -->




<!-- submit start -->
<section class="px-4 py-5 my-5 text-center">
    <img src="https://i.ibb.co/MGwZsL2/publish-blog.png" class="d-block mx-auto mb-4 img-fluid" alt="Publish your blog for free today" width="566" heigh="208" loading="lazy">
    <h1 class="display-5 fw-bold">Publish your blog for free today</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4"> Publish your blog for thousands of people to read for free.</p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <a href="/submit-blog" class="btn btn-primary btn-dark btn-lg">Submit Blog</a>
        </div>
    </div>
</section>
<!-- submit end -->